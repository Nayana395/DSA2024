# SQL
```
JOIN and Natural join
  - combine data in multiple tables
  * SELECT m.name , g.genre
  * FROM movies m
  * JOIN movie_genre g
  * ON  m.id=g.movie_id
  * LIMIT 20;
**INNER JOIN examplew:**

SELECT Table.ID, Table.name, B.course
FROM Table A
INNER JOIN Table B
ON A.ID=B.ID
LIMIT 20;

LEFT JOIN Example
SELECT Table.ID, Table.name, B.course
FROM Table A
LEFT JOIN Table B
ON A.ID=B.ID
LIMIT 20;

RIGHT JOIN Example
SELECT Table.ID, Table.name, B.course
FROM Table A
RIGHT JOIN Table B
ON A.ID=B.ID


FULL OUTER JOIN Example
SELECT Table.ID, Table.name, TableB.course
FROM Table A
FULL OUTER JOIN Table B
ON A.ID=B.ID

NATURAL JOIN Example
SELECT Table.ID, Table.name, TableB.course
FROM Table A
NATURAL JOIN Table B
FULL OUTER JOIN Table B
ON A.ID=B.ID


Practice with Hacker Rank
Sub-queries or Nested Queries or Inner Queries
SELECT actor_id FROM roles WHERE movie_id IN

IN, NOT IN, EXISTS, NOT EXISTS, ANY, ALL,   Comparison operators

CREATE, READ, UPDATE, DELETE (CRUD)
CREATE
  CREATE TABLE table_name(
col1 col1_dtype,
col2 cole2_dtype,
);


insert
INSERT INTO movies(id, name, year, rankscore)VALUES (412321, 'Thor', 2011, 7);

ALTER: IS USED TO MODIFY THE STRUCTURE ofn the table.

DELETE 
DELETE FROM movies WHERE ID=42543543

DROP
dELETE both table and all of the data permenently
